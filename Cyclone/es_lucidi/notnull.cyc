#include <stdio.h>

void f(int @ x){
*x = 10;
return;
}

int main(){
    //int * @notnull p;
    int @ p;
    int x = 1;
    p = &x;
    if (7 > 10) p = NULL;
    f(p);
    printf("%d",x);
	return 0;
}


